import { jsx as _jsx } from "react/jsx-runtime";
import { PresentationWeatherProfile } from './PresentationWeatherProfile';

export const PresentationWeatherProfilePanel = ({ route }) => {
  return _jsx(PresentationWeatherProfile, { route: route });
};
