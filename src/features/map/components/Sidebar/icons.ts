import {
  Route,
  Upload,
  Save,
  FolderOpen,
  Camera,
  MapPin
} from 'lucide-react';

export const SidebarIcons = {
  actions: {
    gpx: Route,
    upload: Upload,
    save: Save,
    load: FolderOpen,
    photos: Camera,
    poi: MapPin
  }
};
